#Novius Blocks - Build instructions

Some of the stylesheets in the app uses grunt + sass.

To change them you need to execute

```npm install```

And then either ```grunt sass``` or ```grunt watch```.